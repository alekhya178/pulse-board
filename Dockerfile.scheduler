FROM node:20-alpine
WORKDIR /app

RUN addgroup -S pulseboard && adduser -S pulseboard -G pulseboard

COPY package*.json ./
RUN npm ci --only=production

COPY . .

USER pulseboard

ENV NODE_ENV=production

CMD ["node", "src/scheduler/scheduler.js"]
